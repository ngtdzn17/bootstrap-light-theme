<!-- Navbar -->
<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
    
    <h5 class="my-0 mr-md-auto font-weight-normal nav-branding">Kleidi.</h5>
    
    <nav class="my-2 my-md-0 mr-md-3">
        <a class="p-2 text-gray {{ 'active' if page == 'home' }}" href="/">Home</a>
        <a class="p-2 text-gray {{ 'active' if page == 'about' }}" href="/p/about">About</a>
        <a class="p-2 text-gray {{ 'active' if page == 'dashboard' }}" href="/dashboard">Dashboard</a>
    </nav>
    
    <a class="btn btn-outline-primary sign-in" href="{{ '/auth/logout' if user else '/auth/discord' }}">{{ 'Sign Out' if user else 'Sign In' }}</a>

</div>